import '../../styles/globals.css'
import '../../styles/reset.css'

const MyApp = ({ Component, pageProps }) => {
  return (
    <>
      <Component {...pageProps} />
    </>
  )
}

export default MyApp
